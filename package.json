{
  "name": "treumarkt-kasa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobil öncelikli satış, gider, kâr takibi — Vanilla JS + Prisma + Neon + Vercel",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "build": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
